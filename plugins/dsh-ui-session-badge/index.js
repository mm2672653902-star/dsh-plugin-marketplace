/**
 * dsh-ui-session-badge — host half (no-op; the widget lives in client.js).
 */
export const name = 'dsh-ui-session-badge'

export function apply() {
  // Browser-only surface; nothing to do on the host.
}
